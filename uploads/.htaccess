# Prevent execution of any script files in this uploads directory.
# This is a basic security measure so that even if a malicious file
# is somehow uploaded, the server will not execute it as PHP.
<FilesMatch "\.(php|phtml|php3|php4|php5|pl|py|cgi|asp|sh)$">
    Require all denied
</FilesMatch>
